# CFG 프로젝트 결과물 요약본 (팀원 공유용)

이 문서는 제가 각 파트별 실험을 위해 작성한 4개의 `cfg_starter` 파일의 내용과 세팅을 정리한 것입니다. 
팀원 여러분은 본인이 맡은 발표 파트 준비 시, 아래 **파일별 세팅과 결과**를 확인하고 필요한 코드나 이미지 결과를 추출하여 본인의 발표 파트로 추합하시면 됩니다.

---

## 📂 파일별 상세 세팅 및 설명

### 1. `cfg_starter (10).ipynb`
*   **학습 성숙도 (Epoch):** `10`
*   **Part 1 (공통):** 손실함수 및 조건/무조건부 샘플링(CFG) 함수 구현. (조건부 샘플링의 기본 가이던스 스케일은 `gamma = 3.0`으로 세팅됨)
*   **Part 2 (Gamma 실험):** Epoch 10인 상태에서 `target_label = 5`에 대해 `gamma` 값을 -1부터 5까지 변화시키며 이미지를 생성한 결과가 들어 있습니다.
*   **Part 3 (Perturbation):** 노이즈 주입 시점을 `t = 100`으로 고정하여 0.1의 교란을 준 뒤 복원하는 테스트 결과가 있습니다.
*   **Part 4 & 5 (Hole Filling / RePaint):** 
    *   **마스킹 비율 실험:** 마스킹 비율을 `0.3, 0.5, 0.7`로 다르게 설정하여 복원하는 코드가 포함되어 있습니다.
    *   **RePaint 노이즈 주입 실험:** 마스킹되지 않은 원본 영역(Good part)을 강제로 유지할 때, 노이즈를 추가하며 자연스럽게 융합하는 방식(`add_noise_to_known=True`)과 노이즈 없이 원본 픽셀만 덮어씌우는 방식(`False`)을 비교하는 심화 실험 결과가 들어 있습니다.

### 2. `cfg_starter(20).ipynb`
*   **학습 성숙도 (Epoch):** `20`
*   **Part 1 (공통):** 동일 (`gamma = 3.0`)
*   **Part 2 (Gamma 실험):** Epoch 20 상태에서 `target_label = 5`에 대한 `gamma` -1 ~ 5 변화 생성 결과.
*   **Part 3 (Perturbation):** `t = 100` 시점의 교란 결과.
*   **Part 4 & 5 (Hole Filling / RePaint):** 기본적인 하단 절반(비율 0.5) 마스킹 후 복원하는 기본 코드만 구현되어 있습니다. (비율 실험 및 Good part 노이즈 추가 실험 코드는 제외됨)

### 3. `cfg_starter(30).ipynb`
*   **학습 성숙도 (Epoch):** `30` 
*   **Part 1 (공통):** 동일 (`gamma = 3.0`)
*   **Part 2 (Gamma 실험):** Epoch 30 상태에서 `target_label = 5`에 대한 `gamma` -1 ~ 5 변화 생성 결과.
*   **Part 3 (Perturbation):** `t = 100` 시점의 교란 결과.
*   **Part 4 & 5 (Hole Filling / RePaint):** 기본적인 하단 절반(비율 0.5) 마스킹 후 복원하는 기본 코드만 구현되어 있습니다. (비율 실험 및 Good part 노이즈 추가 실험 코드는 제외됨)

### 4. `cfg_starter(50).ipynb`
*   **학습 성숙도 (Epoch):** `50`
*   **Part 1 (공통):** 동일 (`gamma = 3.0`)
*   **Part 2 (Gamma 실험):** Epoch 50 상태에서 `target_label = 5`에 대한 `gamma` -1 ~ 5 변화 생성 결과. (가장 충분히 학습된 상태에서의 CFG 효과 및 픽셀 깨짐 분석에 유용합니다.)
*   **Part 3 (Perturbation):** `t = 100` 시점의 교란 결과.
*   **Part 4 & 5 (Hole Filling / RePaint):** 
    *   **마스킹 비율 실험:** 마스킹 비율을 `0.3, 0.5, 0.7`로 다르게 설정하여 복원하는 코드가 포함되어 있습니다.
    *   **RePaint 노이즈 주입 실험:** 마스킹되지 않은 원본 영역(Good part)을 강제로 유지할 때, 노이즈를 추가하는 방식(`add_noise_to_known=True`)과 노이즈 없이 원본 픽셀만 덮어씌우는 방식(`False`)을 비교하는 심화 실험 결과가 들어 있습니다.

---

## 📌 팀원 파트별 자료 취합 가이드

이 문서와 파일들을 바탕으로 각 파트 담당자는 다음을 수행해 주세요.

*   **[Part 1] 코드 채우기:** 4개 파일 중 아무 파일이나 열어서 `Part 1` 섹션의 손실함수/샘플링 코드 구현부를 캡처 및 설명.
*   **[Part 2] Epoch와 Gamma의 상관관계:** 4개 파일(`Epoch 10, 20, 30, 50`)의 `Part 2` 결과 이미지를 모두 모아서 시각적 품질 차이와 최적의 Gamma 값을 도출해 발표 자료로 구성.
*   **[Part 3] Perturbation:** 4개 파일에 있는 `t=100` 결과를 기초로 삼고, 본인이 추가로 가지고 있는 다양한 시점(`t=20, 50, 150 등`) 결과를 합쳐서 임계점(`t=150`) 분석에 활용.
*   **[Part 4 & 5] Hole Filling & RePaint:** 
    *   `10`과 `50` 파일에 있는 **마스킹 비율 (0.3/0.5/0.7) 실험** 결과를 가져와 마스킹 영역 크기에 따른 AI의 상상력 한계를 분석.
    *   `10`과 `50` 파일에 있는 **Good part 노이즈 추가(True/False) 비교 실험** 결과를 가져와, 원본 영역 유지 시 노이즈 주입이 복원 자연스러움에 미치는 영향을 분석하여 최종 결론 도출.
    *   에포크별(`10`~`50`) 기본 복원 능력 차이를 보려면 4개 파일을 모두 대조.
